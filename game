A = ['a', 'A', 'ey', 'AY'] 
B = ['b', 'B', 'bee'] 
C = ['c', 'C', 'see',]
def outside():
    print()
    anw = input('You can, A) walk back up to the door B) Leave the house.') 
    
def door():
    print()
    anw = input('You can, A) walk back up to the door B) Leave the house.') 
    
def hallway():
    print()
    anw = input('You can, A) Go forward B) Turn right C) go back out the door')
    
def room1():
    print()
    anw = input('You can, A) Go forward B) Turn right C) go back out the door')
    
def room2():
    print()
    anw = input('You can, A) Go forward B) Turn right C) go back out the door')
    
def room3():
    print()
    anw = input('You can, A) search room B) look in the closet C) leave the room')
def garage():
    print()
    awn = input('You can A) do something B) do something else C) don a cape and fight crime
        
def seacretroom1():
    print()
    awn = input('You can A) search the room B) go right C) leave the room')
    
def PLAY_GAME():
    print('introduction')
    anw = input('You can, A) walk up to the door B) leave the house ')
    if anw in A:
        door()
    elif anw in B:
        outside()
    else:
        print('please input an answer')
        
        
PLAY_GAME()
